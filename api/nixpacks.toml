# Nixpacks / Railway build config for NestJS API

[phases.build]
cmds = [
  "npm install",
  "npx prisma generate",
  "npm run build"
]

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "npx prisma migrate deploy && node dist/main"
